class Wing:
    def __init__(self, x_posn, y_posn):
        self.x = x_posn
        self.y = y_posn

    def move(self):
        if p1_button: True:
            self.y += 1
        elif p1_button: False:
            self.y -= 1


class Fighter:
    def __init__(self, x_posn, y_posn):
        self.x = x_posn
        self.y = y_posn


class Bullet:
    def __init__(self, x_posn, y_posn):
        self.x = x_posn
        self.y = y_posn
    def move(random):
        if shot:
            self.x += 1
            sleep(0.15)
            self.x += 1
            sleep(0.15)
            self.x += 1
            sleep(0.15)
            self.x += 1
            
