class Wing:
    def __init__(self, x_posn, y_posn):
        self.x = x_posn
        self.y = y_posn

class Fighter:
    def __init__(self, x_posn, y_posn):
        self.x = x_posn
        self.y = y_posn


class Bullet:
    def __init__(self, x_posn, y_posn):
        self.x = x_posn
        self.y = y_posn

    def move(random):
        if shot:
            self.x += 1
            sleep(0.15)
            self.x += 1
            sleep(0.15)
            self.x += 1
            sleep(0.15)
            self.x += 1

